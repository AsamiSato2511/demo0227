<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.SubjectMapper">

    <select id="findAll" resultType="Subject">
        SELECT id, field_name, major_name, minor_name, color
        FROM subjects
        ORDER BY field_name, major_name, minor_name
    </select>

    <select id="findById" parameterType="long" resultType="Subject">
        SELECT id, field_name, major_name, minor_name, color
        FROM subjects
        WHERE id = #{id}
    </select>

    <update id="mergeFromCategory">
        MERGE INTO subjects (field_name, major_name, minor_name, color)
        KEY (field_name, major_name, minor_name)
        VALUES (#{fieldName}, #{majorName}, #{minorName}, #{color})
    </update>

</mapper>
